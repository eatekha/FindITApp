//
//  ContentView.swift
//  FindIT
//
//  Created by Eseosa on 2023-11-13.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("YO THIS IS VERY COOL, TO FIGURE OUT WHERE WE GO FROM THERE!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
